# Core scientific stack
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.0.0
matplotlib>=3.8.0
seaborn>=0.13.0

# EEG / MNE
mne>=1.6.0

# Wavelets (MODWT/SWT)
PyWavelets>=1.5.0

# Machine learning
scikit-learn>=1.4.0
pyriemann>=0.6          # Riemannian covariance metrics (optional)

# BCI datasets
moabb>=1.0.0

# Artifact rejection baseline
autoreject>=0.4.0       # optional AutoReject comparison

# Statistical tests
statsmodels>=0.14.0     # FDR correction

# Utilities
pyyaml>=6.0             # YAML config parsing
tqdm>=4.66              # Progress bars
joblib>=1.3.0           # Parallel processing

# Development / testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Optional: faster matrix ops on Apple Silicon
# scipy is already accelerated via Accelerate framework on macOS
# For GPU support: install cupy separately
